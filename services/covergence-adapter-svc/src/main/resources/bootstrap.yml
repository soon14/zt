spring:
  application:
    name: covergence-adapter-svc
  cloud:
    config:
      name: eureka-covergence-adapter-svc
      label: master
      profile: test
      enabled: true
      fail-fast: false
      retry:
        max-attempts: 6
    bus:
      trace:
        enabled: true