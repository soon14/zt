spring:
  application:
    name: schedule-svc
  cloud:
    config:
      name: eureka-schedule-svc
      label: master
      profile: test
      enabled: true
      fail-fast: false
      retry:
        max-attempts: 6
    bus:
      trace:
        enabled: true